name: Build

on:
  push:
  pull_request:

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: "-Dwarnings"

jobs:
  check:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: Swatinem/rust-cache@v2
      - name: Check formatting
        run: |
          rustfmt --version
          cargo fmt --all -- --check
      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y  openssl libssl-dev pkg-config ffmpeg yasm build-essential wget libbz2-dev zlib1g-dev libavformat-dev
      - name: Check ffmpeg versions
        run: |
          ffmpeg -version
          pkg-config --modversion libavformat
          PKG_CONFIG_ALLOW_SYSTEM_LIBS=1 PKG_CONFIG_ALLOW_SYSTEM_CFLAGS=1 pkg-config --libs --cflags libavformat
      - name: Check compilation
        run: |
          rustc --version
          cargo check --all --features transcoding-cache,tags-encoding,collation
      - name: Clippy check
        run: cargo clippy --all --features transcoding-cache,tags-encoding,collation
      - name: Run unit tests
        run: cargo test --all --features transcoding-cache,tags-encoding,collation
      - name: Install cargo-llvm-cov
        uses: taiki-e/install-action@cargo-llvm-cov
      - name: Generate code coverage
        run: cargo llvm-cov --all --lcov --output-path lcov.info
      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v6
        with:
          token: ${{ secrets.CODECOV_TOKEN }} # not required for public repos
          files: lcov.info
          fail_ci_if_error: true

  integration-test:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: Swatinem/rust-cache@v2
      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y  openssl libssl-dev pkg-config ffmpeg yasm build-essential wget libbz2-dev zlib1g-dev libavformat-dev
      - name: Run integration tests
        run: cargo test --features transcoding-cache,tags-encoding -- --ignored --nocapture test_binary
